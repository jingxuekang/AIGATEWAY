apiVersion: 1

datasources:
  - name: Elasticsearch
    type: elasticsearch
    uid: ffg8whlt2tn28f
    access: proxy
    url: http://localhost:9200
    database: usage_log
    basicAuth: false
    isDefault: false
    jsonData:
      esVersion: "8.0.0"
      timeField: timestamp
      maxConcurrentShardRequests: 5
    editable: true
